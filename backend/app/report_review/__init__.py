"""报告复核 (Report Review) 模块.

接受审计师上传的待复核报告（单体 / 合并，可多文件，Word + Excel），
结合复核要求（文字或上传的 .md 文档），用 LLM 执行复核，
返回结构化复核意见，并把每条意见 link 回原文（文件 + 段落 / sheet 单元格）。

无 GITHUB_TOKEN 时进入 DEMO 模式，返回一份基于真实审计报告内容的确定性复核结果。
"""
